import { useState } from "react";
import Dashboard from "../features/Dashboard/Dashboard";
const Interface = () => {
  const [value, setValue] = useState(Interface);

  return <Dashboard />;
};

export default Interface;
